Run the full test suite with `pytest --tb=short`. Report results: number passed, failed, skipped. Compare against the baseline in `docs/TESTING.md` ("Current status" line). ALL failures must be investigated and fixed — there is no known-failing whitelist. After fixing, re-run to confirm. If the passed count is higher than the baseline, note "update baseline in docs/TESTING.md".
