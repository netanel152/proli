[pytest]
markers =
    integration: marks tests as integration tests (requires real DB)
asyncio_mode = strict
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestRemovedIn9Warning