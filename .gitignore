# --- Security (NEVER COMMIT THESE) ---
.env
.env.local
secrets.json

# --- Python ---
__pycache__/
*.py[cod]
*.class
*.so

# --- Virtual Environment ---
venv/
.venv/
env/

# --- IDE / Editor ---
.vscode/
.idea/
*.swp

# --- OS Files ---
.DS_Store
Thumbs.db

# --- Logs ---
*.log
proli.log
logs/
*.zip

settings.local.json

# --- Claude Code local state (not for teammates) ---
.claude/worktrees/
.claude/agent-memory/
# claude-flow rewrites its neural/policy state every session — pure local
# churn that used to show up as dirty-tree noise in every PR.
.claude-flow/

# --- Local data & caches (never commit) ---
# backups/ holds real mongodump output (customer data) - PRO-144
backups/
.pytest_cache/
