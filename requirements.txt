# Core Backend
fastapi~=0.115.0
uvicorn==0.27.1
gunicorn==21.2.0
# PRO-50: pin pydantic AND pydantic-core together. pydantic-core was previously
# unpinned, so it could float to a version incompatible with the pinned pydantic
# (e.g. pydantic 2.6.1 + pydantic-core 2.46.3 → "cannot import name
# 'validate_core_schema'"), which aborted conftest import outside the venv. Both
# are pinned to the proven-compatible pair the suite runs green on.
pydantic==2.6.1
pydantic-core==2.16.2
# pydantic-settings is pinned too: ~=2.1 would let it float to >=2.6, which
# requires pydantic>=2.7 and conflicts with the pin above — a fresh install
# would then backtrack pydantic-settings nondeterministically (the exact drift
# PRO-50 fixes). 2.1.0 is the proven-compatible installed version.
pydantic-settings==2.1.0
python-dotenv==1.0.1
httpx~=0.27.0
tenacity==8.2.3
jinja2==3.1.3

# Database
motor==3.3.2
pymongo==4.6.1
certifi==2024.2.2

# AI & Machine Learning
# Pinned, not `~=1.0`. google-genai >=1.3 requires pydantic>=2.9 and httpx>=0.28,
# both of which conflict with the pins above, so pip silently backtracked `~=1.0`
# down to 1.2.0 anyway — a resolution nobody could see from this file. Pinning it
# makes the *actual* production version explicit: whatever schema quirks 1.2.0 has
# (see the AIResponse.extracted_data note in app/services/ai_engine_service.py)
# are the ones the code is written against. To move off 1.2.0, bump pydantic to
# >=2.9 and httpx to >=0.28 in the same change.
google-genai==1.2.0
cloudinary==1.38.0

# Scheduling & Logging
APScheduler==3.10.4
loguru==0.7.2
pytz==2024.1

# Admin Panel (Streamlit)
streamlit==1.31.1
extra-streamlit-components==0.1.71
# PRO-141 — client-side auto-refresh timer. st.fragment(run_every=...) would
# be the modern answer but needs Streamlit >= 1.37; this component works
# against the 1.31.1 pin above (it requires only streamlit >= 0.75).
streamlit-autorefresh==1.0.1
pandas==2.2.0
bcrypt==4.1.2

# Testing & Dev
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-xdist==3.6.1  # `-n auto` for ad-hoc local runs only; the authoritative run (CI, test-runner) stays serial for order-dependency parity
mongomock-motor==0.0.26
fakeredis~=2.37  # PRO-78: in-memory Redis so unit tests never touch a real server
black==24.1.1
flake8==7.0.0
watchdog==3.0.0

# Seeding
Faker==23.2.0

# Task Queue & Caching
redis~=5.0
arq~=0.25

# Backup (optional S3 upload)
boto3~=1.34

# Error Reporting (optional — worker only, activates only when SENTRY_DSN is set)
sentry-sdk~=2.68
